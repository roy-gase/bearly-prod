# Vercel serverless runtime installs this at the project root.
# Mirrors backend/requirements.txt minus the container-only pieces
# (gunicorn and uvicorn are not used: Vercel supplies the ASGI server).
fastapi==0.115.6
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
python-multipart==0.0.20
PyJWT==2.10.1
bcrypt==4.2.1
httpx==0.28.1
anthropic>=0.42.0
